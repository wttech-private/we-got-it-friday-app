{
  "name": "friday-app",
  "version": "0.0.0",
  "scripts": {
    "percy:local": "npx percy exec -- cypress run --config-file cypress/config/local.ts --browser chrome",
    "percy:int": "npx percy exec -- cypress run --config-file cypress/config/int.ts --browser chrome",
    "percy:prod": "npx percy exec -- cypress run --config-file cypress/config/prod.ts --browser chrome"
  },
  "devDependencies": {
    "@percy/cli": "^1.20.0",
    "@percy/cypress": "^3.1.2",
    "@shelex/cypress-allure-plugin": "^2.35.2",
    "@tsconfig/node16": "1.0.3",
    "@types/cypress": "^1.1.3",
    "@types/node": "^18.14.1",
    "cypress": "^12.6.0",
    "typescript": "4.8.4"
  }
}
