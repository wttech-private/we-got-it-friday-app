{
  "name": "friday-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "sst bind -- vitest run",
    "percy:dev": "cd tests && npx percy exec -- cypress run"
  },
  "devDependencies": {
    "@percy/cli": "^1.20.0",
    "@percy/cypress": "^3.1.2",
    "@serverless-stack/cli": "1.16.1",
    "@serverless-stack/resources": "1.16.1",
    "@tsconfig/node16": "1.0.3",
    "aws-cdk-lib": "2.39.1",
    "cypress": "^12.6.0",
    "typescript": "4.8.4",
    "vitest": "0.24.5"
  },
  "workspaces": [
    "services",
    "frontend"
  ]
}
